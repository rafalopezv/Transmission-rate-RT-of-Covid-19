df_mundo %>%
  mutate(
    millon = casos_acumulados/poblacion * 1000000,
    millon = round(millon, 0), 
    base = str_to_title(base)
  ) %>% 
  filter(fecha == "2020-08-10") %>% 
  filter(base == "Fallecidos") %>% 
  arrange(desc(millon)) %>% 
  mutate(posicion = 1:nrow(.)) %>% 
  view
  

